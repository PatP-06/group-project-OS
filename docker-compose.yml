services:
  server:
    build: .
    container_name: os-server
    ports:
      - "8080:8080" # Web Dashboard (Browser)
      - "8081:8081" # TCP Socket (Client.java)
    volumes:
      - ./:/workspace
    restart: unless-stopped
